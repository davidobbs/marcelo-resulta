'use client';

// This component is intentionally kept simple.
// The redirect from the root path ("/") to "/dashboard" is handled
// by the `redirects` configuration in `next.config.js`.
// This page should ideally not be reached.
export default function HomePage() {
  return null;
}